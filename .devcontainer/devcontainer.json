{
  "name": "QEMU KVM Environment",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "runArgs": [
    "--device=/dev/kvm"
  ],
  "postCreateCommand": "sudo chmod 666 /dev/kvm && sudo apt-get update && sudo apt-get install -y qemu-system-x86 cpu-checker"
}
